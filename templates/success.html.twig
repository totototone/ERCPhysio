<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}
        <p>{% block text%}

            Utilisateurs ajoutés : <br>
            {% for user in reussis %}
                {{user.username}} <br>
            {% endfor %}
             <br> <hr> <br>
            Utilisateurs déjà existants, pas ajoutés :
            {% for user in rates %}
                {{user.username}} <br>
            {% endfor %}
        {% endblock %}</p>
        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
